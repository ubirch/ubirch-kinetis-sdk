/*!
@page middleware_log Middleware Change Log

@section emvl1 EMVL1 for KSDK
    Current driver version is 2.0.1

    - 2.0.0
        - Initial version.

    - 2.0.1
        - Bug Fix:
            - fixed low level driver protocol timers failures during emvl1 pre-certification tests (KPSDK-9556).
            - fixed improper T0 commands response receiving (commands case2, case3 & case4 affected)
                what causes long commands responses (KPSDK-8707).
*/
